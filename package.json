{
  "name": "orad-ghipss",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client --include=dev && npm run build --prefix client && npm install --prefix server && cd server && npx prisma migrate deploy && npx prisma generate && npx prisma db seed"
  }
}
